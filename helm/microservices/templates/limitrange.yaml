apiVersion: v1
kind: LimitRange
metadata:
  name: microservices-limit-range
  namespace: {{ .Values.namespace }}
spec:
  limits:
  - default:
      cpu: 200m
      memory: 256Mi
    defaultRequest:
      cpu: 50m
      memory: 128Mi
    type: Container
